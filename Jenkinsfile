pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'MiniMart project checked out successfully'
            }
        }

        stage('Build') {
            steps {
                bat 'dir'
            }
        }
    }
}
