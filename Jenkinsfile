Jenkinsfile
pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'MiniMart project checked out successfully'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage completed'
            }
        }
    }
}
